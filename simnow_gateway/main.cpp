#include <iostream>
int main(int argc, char**) {
  std::cout << "simnow_gateway: CTP adapter placeholder (SDK integration pending)\n";
  if (argc < 2) std::cerr << "usage: simnow_gateway CONFIG.toml\n";
  return 0;
}
